#include "main.h"
/**
 * add - Adds two integers and returns
 * the result.
 *
 * @a: first number.
 * @b: second number.
 *
 * Return: Result of adding first and second numbers.
 */
int add(int a, int b)
{
	return (a + b);
}
